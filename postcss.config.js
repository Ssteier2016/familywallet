module.exports = {
  // Utilizamos el formato de objeto, que es el recomendado
  // para las versiones modernas de PostCSS y Tailwind CSS (PostCSS 8+)
  plugins: {
    'tailwindcss': {},
    'autoprefixer': {},
  }
}
